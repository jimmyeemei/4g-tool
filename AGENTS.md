# Project Agent Guide

本文件用于帮助后续维护者或代码代理快速进入这个项目。

## 项目定位

这是一个面向基站 Excel 配置场景的长期维护项目，不是一次性脚本。任何改动都应优先考虑：

- 是否会破坏现有模板兼容性
- 是否会影响中文文件名、sheet 名、表头名
- 是否会影响现场人员的操作路径

## 工作原则

1. 先核对真实模板，再改代码。
2. 先确认表头、起始行、目标行数规则，再写填表逻辑。
3. 不要凭终端乱码直接判断字段不存在。
4. 对 Excel 的批量改动要尽量保留样式、字体、行高、原始模板结构。

## 修改优先级

### 高优先级

- 填表逻辑是否正确
- 行数是否符合现场规则
- 错误提示是否能定位到 sheet / 列 / 参数

### 中优先级

- 代码拆分
- 命名统一
- 结果文件名清晰度

### 低优先级

- 页面样式
- 文案微调

## 关键模块说明

### `app.py`

- 只负责主页和工具分发
- 不要把复杂 Excel 逻辑堆回主页

### `kaizhan/`

- 保持与原有开站逻辑兼容
- 尽量减少无关重构
- 代码已经能正确运用，尽量别碰此代码文件

### `kuorong/app_sdr_expansion.py`

- 是扩容规则主实现
- 任何新增 sheet 规则都应优先抽成独立处理函数
- 若涉及跨制式逻辑，要同时检查 `RANCM` 与 `cfgRadioNet`

## 处理 Excel 的约束

- 保留原表头和样式
- 行数不足时按规则复制同列模板值
- 行数超出时删除多余行
- 源数据不够时必须提示具体位置

## 推荐交付方式

- 代码改动
- 对应规则说明
- 关键风险提示
- 简短验证结论
